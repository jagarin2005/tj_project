pipeline {
  agent { docker: 'php' }
  stages {
    stage('build') {
      steps {
        sh 'php --version'
      }
      steps {
        sh 'npm install'
      }
      steps {
        sh 'npm run build'
      }
    }
  }
}
